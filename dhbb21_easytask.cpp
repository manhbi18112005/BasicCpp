#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> n;
    int a[n];
    if(n>=2) {
        for (int i=1;i<=n;i++) {
            cin >> a[i];

        }
    }
    else {
        return 0;
    }
    return 0;
}
