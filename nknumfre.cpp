#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int a,b;
    cin >> a >> b;
    for(int i = a; i <= b; i++) {
        int dem = 0;
        if(i>b) {
            cout << dem;
            return 0;
        }
        else {
            for(int k = i; )
        }

    }
    return 0;
}
